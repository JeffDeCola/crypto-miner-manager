# crypto-miner-manager task-deploy.yml

platform: linux

# DOCKER BASE IMAGE FOR TASKS (BASED ON ALPINE, A SMALLER LINUX DISTRIBUTION)
image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.20.1

params:
  DOCKER_HOST_SSH_PRIVATE_KEY: "will be replaced"
  DOCKER_HOST_PORT: "will be replaced"
  DOCKER_HOST_USER: "will be replaced"
  DOCKER_HOST_IP: "will be replaced"

inputs:
- name: crypto-miner-manager

run:
  path: ./crypto-miner-manager/ci/scripts/deploy.sh
  # args: [-debug]

